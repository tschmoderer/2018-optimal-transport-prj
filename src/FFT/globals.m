%% define flobals variables

global N; % nb de points de discrétatisation dans la direction x
global P; % nb de points de discrétatisation dans la direction y
global Q; % nb de points de discrétatisation dans la direction t

global f0; % densité initiale
global f1; % densité initiale

%% Data structure %%
% w(:,:,:,1:2) = vector m at (y,x,t)
% w(:,:,:,3)   = field f at (y,x,t)
% 
% 
%%

